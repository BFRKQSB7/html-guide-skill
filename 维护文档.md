# html-guide 维护文档

## 项目概览与目标

Claude Code skill：把任意内容（对话回答/笔记/代码/主题/URL）自动生成一份**自包含、可交互、可打印、现代化视觉**的单 HTML 指导文档（教程/指南/讲解页/操作手册/学习路径），页面结构随内容自适应。**边界**：只生成单个 `.html` 成品，不做文档托管、不做站点生成器。

## 维护状态

个人维护项目，按需更新，PR 欢迎但不保证及时。**发布文件禁带本机个性化信息**是硬门禁（历史教训：v2.2.1/2.2.4 曾清用户名路径/代理端口）。README 在版本格式上还经历了 v2.2.3/2.2.4 → v2.3.0 的版本折叠，改版本号时注意对齐。

## 构建 / 运行 / 测试

- **安装**：`html-guide` 目录（根 `SKILL.md`）放到 skills 目录，或从 Releases 装源码包
- **运行**：触发生成后输出单个 `index.html`（自包含、无 CDN）；`scripts/check_html.py` 做成品自检（自包含/打印/目录/高亮/无障碍）
- **测试**：`evals/` 评测用例集（skill-creator 规范，6 类用例断言通过率 100%，核心交互经无头 Chrome 真实渲染验证；9 主题 × 6 卡片布局 × 深浅色逐一核对）
- **长文成图**：`scripts/make_a4.py` A4 分页（长文转多张统一 A4 图，表格不切）

## 目录结构 / 代码地图

| 路径 | 职责 |
|------|------|
| `SKILL.md` | 主 skill：完整生成工作流（触发→骨架→内容→自检→清理） |
| `index.html` | 技能介绍页 = 本 skill 的高质量示范成品（GitHub Pages 托管） |
| `assets/` | 分享摘要卡片模板（1200×630）等资源 |
| `references/` | 方法论：联网核实时效、lint 强化、个性化配置（user-config）规范 |
| `user-config.example.md` | 用户个性化配置模板（代理端口等不进发布文件） |
| `scripts/check_html.py` | 成品自检（自包含/打印/目录/高亮/无障碍） |
| `scripts/make_a4.py` | A4 分页成图 |
| `evals/` | 评测用例集 + 断言基线 |
| `docs/en/` | 英文 README（文件切换版双语） |
| `LICENSE` | MIT |

## 发布流程（对齐 improve skill）

1. 改版本号：**改版本必须 `grep vX.Y.Z` 全改**（README 标题/徽章/变更日志），改完复查零残留
2. README「变更日志」节顶部加新版本段（分类词：新增/修复/删除/优化/版本），中英 README 同步
3. 全局 grep 旧版本零残留；**扫描发布文件本机个性化信息（用户名路径/代理端口 7896/user-config 残留）零残留**
4. commit（作者 `226671264+BFRKQSB7@users.noreply.github.com`）→ tag `v{版本号}` → push → `gh release create`

## 提交规范

Conventional Commits 轻量 5 前缀：`feat` / `fix` / `docs` / `refactor` / `chore`。小更新+距上次不久可覆盖发布（重打同 tag）。

## 版本纪律

semver：破坏性→x+1、新功能→y+1、修 bug→z+1。tag 与 README 版本号同步。历史 tag：v1.0.0、v2.0.0~v2.4.0（v2.0.x 系列 9 个小版本，变更日志节已合并写成 `v2.0.x` 一段）。

## 本仓库特有坑 / 注意

- **发布文件零个性化**：SKILL.md 生成流程里硬规则是临时文件清理 + 发布扫描，改模板/流程时保持这两道门禁
- **版本折叠先例**：2.2.3/2.2.4 曾折叠进 v2.3.0——小补丁连续发版可合并为一个 tag，但 README 变更日志要用折叠后的版本号（`v2.3.0` 代表 2.2.3+2.2.4）
- `index.html`（介绍页）由 html-guide 自身生成，修改后建议用 `check_html.py` 自检再发布，Positioned on Pages 时注意同步